import { Miltonian_Tattoo, Miltonian, Ribeye, Ribeye_Marrow  } from 'next/font/google';

export const solid = Miltonian_Tattoo({ subsets: ['latin'], weight: ['400'], display: 'swap' });

export const outline = Miltonian({ subsets: ['latin'], weight: ['400'], display: 'swap' });

export const ribeye = Ribeye({ subsets: ['latin'], weight: ['400'], display: 'swap' });

export const marrow = Ribeye_Marrow({ subsets: ['latin'], weight: ['400'], display: 'swap' });